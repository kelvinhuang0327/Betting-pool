# Inventory: Markdown files (full classification)

Purpose

Machine-readable inventory of markdown files and consolidation actions is maintained as part of the deliverable. This page points to the final mapping and explains where to find the per-file decisions.

Location of full inventory (generated):
- `build/runtime_artifacts/markdown_inventory.csv` (if present)
- Otherwise see the consolidation report in the transcript or request a CSV export.
