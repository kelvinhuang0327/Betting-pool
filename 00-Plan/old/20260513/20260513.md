以下是下一輪 **24H Prompt**。重點：**不要繼續在錯誤工作區 `Betting-pool` 操作**，必須先切回正確 repo：`/Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13`。目前 P30 的 T12 已完成，但 **T13 commit 被取消，尚未完成**，所以下一輪只需要做「狀態校驗 → staged/unstaged 檢查 → commit 13 檔案 → 確認 marker」，不要重做 P30 全流程，也不要前進 P31。

---

# Next 24H Prompt — P30 Commit Recovery / Workspace Correction

```text
# ROLE
You are Betting-pool's P30 Commit Recovery Agent.

# MISSION
Recover and finish the interrupted P30 commit step.

Current state from previous session:
- The previous operation was cancelled during `git commit`.
- T12 final report was already completed.
- T13 commit of exactly 13 P30 files was NOT completed.
- Current working directory may be WRONG:
  - Current workspace observed: /Users/kelvin/Kelvin-WorkSpace/Betting-pool
  - Correct required workspace: /Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13

This task is ONLY to:
1. switch to the correct repo,
2. verify P30 files/report exist,
3. stage exactly the intended 13 files,
4. run forbidden-file check,
5. commit,
6. report the final state.

Do NOT rerun P30 from scratch unless files are missing.
Do NOT advance to P31.
Do NOT push.

# PROJECT LOCK
Required repo:
  /Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13

Required branch:
  p13-clean

Forbidden repo for this task:
  /Users/kelvin/Kelvin-WorkSpace/Betting-pool

Required prior marker:
  P29_SOURCE_COVERAGE_DENSITY_EXPANSION_BLOCKED

Expected P30 final report:
  00-BettingPlan/20260512/p30_historical_source_acquisition_plan_report.md

Expected P30 marker, one of:
  P30_HISTORICAL_SOURCE_ACQUISITION_PLAN_READY
  P30_HISTORICAL_SOURCE_ACQUISITION_PLAN_BLOCKED

# HARD GUARD
DO NOT:
- push
- open PR
- modify branch protection / workflows
- write production DB
- call live TSL
- scrape live odds
- place real bets
- claim production readiness
- enable cron / daemon / production scheduler
- fabricate source artifacts
- run pull / reset / stash / clean
- commit DB binaries, runtime/, .venv/, outputs/, or large generated files
- commit from /Users/kelvin/Kelvin-WorkSpace/Betting-pool

# TASKS

## Task 1 — Correct workspace verification
Run:

  cd /Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13
  pwd
  git rev-parse --show-toplevel
  git branch --show-current
  git log --oneline -12
  git status --short --branch | head -100

Acceptance:
- `pwd` and git root must both be:
  /Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13
- branch must be:
  p13-clean

If repo or branch is wrong, STOP and report context drift.

## Task 2 — Verify P29 prior marker
Run:

  grep "P29_SOURCE_COVERAGE_DENSITY_EXPANSION_BLOCKED" \
    00-BettingPlan/20260512/p29_source_coverage_density_expansion_report.md

If missing, STOP and report marker mismatch.

## Task 3 — Verify P30 report and marker
Run:

  ls -l 00-BettingPlan/20260512/p30_historical_source_acquisition_plan_report.md

  grep -E "P30_HISTORICAL_SOURCE_ACQUISITION_PLAN_READY|P30_HISTORICAL_SOURCE_ACQUISITION_PLAN_BLOCKED" \
    00-BettingPlan/20260512/p30_historical_source_acquisition_plan_report.md

If report exists and marker exists:
- Do NOT rerun P30.
- Proceed to commit.

If report is missing:
- STOP and report that T12 artifact is not present in the correct repo.
- Do not recreate unless explicitly instructed.

## Task 4 — Confirm P30 files exist
Run:

  ls \
    wbc_backend/recommendation/p30_source_acquisition_contract.py \
    wbc_backend/recommendation/p30_historical_season_source_inventory.py \
    wbc_backend/recommendation/p30_required_artifact_spec_generator.py \
    wbc_backend/recommendation/p30_source_acquisition_plan_builder.py \
    wbc_backend/recommendation/p30_dry_run_artifact_builder_skeleton.py \
    scripts/run_p30_historical_source_acquisition_plan.py \
    tests/test_p30_source_acquisition_contract.py \
    tests/test_p30_historical_season_source_inventory.py \
    tests/test_p30_required_artifact_spec_generator.py \
    tests/test_p30_source_acquisition_plan_builder.py \
    tests/test_p30_dry_run_artifact_builder_skeleton.py \
    tests/test_run_p30_historical_source_acquisition_plan.py \
    00-BettingPlan/20260512/p30_historical_source_acquisition_plan_report.md

If any file is missing:
- STOP and list missing files.
- Do not commit partial P30.

## Task 5 — Optional quick test sanity if not already recorded
Only if needed, run focused quick tests:

  ./.venv/bin/pytest -q \
    tests/test_p30_source_acquisition_contract.py \
    tests/test_p30_historical_season_source_inventory.py \
    tests/test_p30_required_artifact_spec_generator.py \
    tests/test_p30_source_acquisition_plan_builder.py \
    tests/test_p30_dry_run_artifact_builder_skeleton.py \
    tests/test_run_p30_historical_source_acquisition_plan.py

If tests fail:
- STOP and report failure.
- Do not commit.

If previous session already clearly recorded passing tests and code has not changed, this step may be skipped, but mention it was skipped because only commit recovery is being performed.

## Task 6 — Stage exactly 13 files
Run:

  git add \
    wbc_backend/recommendation/p30_source_acquisition_contract.py \
    wbc_backend/recommendation/p30_historical_season_source_inventory.py \
    wbc_backend/recommendation/p30_required_artifact_spec_generator.py \
    wbc_backend/recommendation/p30_source_acquisition_plan_builder.py \
    wbc_backend/recommendation/p30_dry_run_artifact_builder_skeleton.py \
    scripts/run_p30_historical_source_acquisition_plan.py \
    tests/test_p30_source_acquisition_contract.py \
    tests/test_p30_historical_season_source_inventory.py \
    tests/test_p30_required_artifact_spec_generator.py \
    tests/test_p30_source_acquisition_plan_builder.py \
    tests/test_p30_dry_run_artifact_builder_skeleton.py \
    tests/test_run_p30_historical_source_acquisition_plan.py \
    00-BettingPlan/20260512/p30_historical_source_acquisition_plan_report.md

Then run:

  echo "=== staged count ==="
  git diff --cached --name-only | wc -l

  echo "=== staged files ==="
  git diff --cached --name-only

Expected staged count:
  13

If staged count is not 13, STOP and report.

## Task 7 — Forbidden file check
Run:

  git diff --cached --name-only | grep -E "\.(db|db-wal|db-shm|sqlite|sqlite3)$|^runtime/|^outputs/|^\.venv/" || echo "NO_FORBIDDEN_FILES"

Expected:
  NO_FORBIDDEN_FILES

If forbidden files appear:
- Unstage them only if safe and explicit.
- Otherwise STOP and report.
- Do not commit.

## Task 8 — Commit
Run:

  git commit -m "feat(betting): plan P30 historical season source acquisition"

Do NOT push.

## Task 9 — Final verification
Run:

  git log --oneline -3
  git status --short --branch | head -100
  git diff --cached --name-only

Expected:
- New commit exists.
- No staged changes remain.
- Untracked/generated outputs may exist, but committed scope is exactly 13 files.
- No push performed.

# FINAL OUTPUT
Return:
1. Correct repo path verified
2. Branch name
3. Commit hash
4. P30 marker found in report
5. Staged file count before commit
6. Forbidden-file check result
7. Test sanity result, if run or skipped
8. Final git status summary
9. Confirmation: NOT PUSHED
10. Next recommended phase

# FINAL MARKER
P30_COMMIT_RECOVERY_COMPLETE
```

---

# 給 CTO Agent 的 10 行內摘要

1. 上一輪不是卡住，而是使用者取消了 `git commit`。
2. 目前風險是工作區錯誤：現在在 `Betting-pool`，正確應回到 `Betting-pool-p13`。
3. P30 的 T12 final report 已完成，但 T13 commit 尚未完成。
4. 下一輪不要重跑 P30，也不要進 P31；先做 commit recovery。
5. 必須驗證 repo root = `/Users/kelvin/Kelvin-WorkSpace/Betting-pool-p13`，branch = `p13-clean`。
6. 需確認 P29 marker 與 P30 report marker 存在。
7. 只 staging 指定 13 個 P30 檔案，禁止 outputs / runtime / DB / .venv。
8. commit message 使用：`feat(betting): plan P30 historical season source acquisition`。
9. 不 push、不開 PR、不做 live TSL、不碰 production DB。
10. commit 完成後下一階段才是 P31：Build 2024 Historical Joined Input Artifacts 或 source acquisition follow-up。
